import { SqlConfig } from '../operations/connect_to_server';

export const sqlConfig: SqlConfig = {
    user: 'user',
    password: 'password',
    host: '1.2.3.4',
    port: 5432,
    database: 'database'
};
